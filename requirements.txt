# Core API Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# HTTP Requests and Data Fetching
requests>=2.31.0
python-dotenv>=1.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Geospatial Data (optional - only if needed for advanced GIS)
# geopandas>=0.14.0
# shapely>=2.0.0

# Testing and Development
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0

# Logging
python-json-logger>=2.0.0

# Date/Time utilities (standard library - no install needed)
# datetime

# For caching
# redis>=5.0.0 (optional - only if using Redis cache)

# ============================================================================
# OPTIONAL DEPENDENCIES FOR ADVANCED FEATURES
# ============================================================================

# Deep Learning (only if keeping existing species detection models)
# tensorflow>=2.13.0
# keras>=2.13.0
# torch>=2.0.0
# torchvision>=0.15.0
# opencv-python>=4.8.0

# Database (only if adding persistence)
# sqlalchemy>=2.0.0
# psycopg2-binary>=2.9.0

# Visualization dashboard (only if building Dash frontend)
# plotly>=5.17.0
# dash>=2.14.0
# matplotlib>=3.7.0
# seaborn>=0.12.0

# Social media data collection (only if needed)
# beautifulsoup4>=4.12.0
# tweepy>=4.14.0
# praw>=7.7.0

# Earth Engine (only if using Google Earth Engine)
# earthengine-api>=0.1.370

# Satellite data (only if downloading satellite imagery)
# sentinelsat>=1.2.0

# Background tasks (only if implementing async processing)
# celery>=5.3.0